# Write a program that takes the name of a movie from the user, capitalizes only the first letter, and displays the result.

name = input("Enter the name: ").lower().title()
print(f"- Result: {name}")