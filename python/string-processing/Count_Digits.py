# Write a program that receives a number from the user
# and prints how many digits the number has.

number_of_digits = len(input("Enter a Number: "))

print(f"Number of digits: {number_of_digits}")